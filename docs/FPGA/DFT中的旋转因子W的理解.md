---
title: "DFT中的旋转因子W的理解"
author:
  - "[[vi226035]]"
created: 2026-07-06
tags:
  - "FPGA"
  - "信号处理"
  - "DFT"
  - "FFT"
---

# DFT中的旋转因子W的理解

## 定义

旋转因子（Twiddle Factor）$W_N$ 是单位周期复指数序列，定义为：

$$W_N = e^{-j\frac{2\pi}{N}}$$

N点DFT中，旋转因子的一般形式为：

$$W_N^{nk} = e^{-j\frac{2\pi}{N}nk}$$

其中：
- $N$ — DFT变换长度（N点DFT）
- $n$ — 时域序列样值的序号，$0 \leq n \leq N-1$
- $k$ — 频域序号（第k个频率采样点），$0 \leq k \leq N-1$

## 几何直观：单位圆等分

![[../kb_material/FPGA_image/dft_w_factor_2.png]]

$W_N$ 表示在复平面上单位圆的一个点，下标 $N$ 是 $2\pi$ 的分母，表示**把单位圆 $N$ 等分**，每一份的角度是 $\frac{2\pi}{N}$。

这个基本角度间隔我们叫它**基频**。

### N=4 举例

![[../kb_material/FPGA_image/dft_w_factor_1.png]]

以 $N=4$ 为例：

$$W_4^0 = e^{-j\frac{2\pi}{4} \cdot 0} = e^0 = 1$$

$$W_4^1 = e^{-j\frac{2\pi}{4} \cdot 1} = e^{-j\frac{\pi}{2}} = \cos(-\frac{\pi}{2}) + j\sin(-\frac{\pi}{2}) = -j$$

$$W_4^2 = e^{-j\frac{2\pi}{4} \cdot 2} = e^{-j\pi} = -1$$

$$W_4^3 = e^{-j\frac{2\pi}{4} \cdot 3} = e^{-j\frac{3\pi}{2}} = j$$

在单位圆上对应四个点：$(1, 0)$、$(0, -j)$、$(-1, 0)$、$(0, j)$，正好将圆四等分。

## 物理意义：频域采样间隔

**旋转因子就是DFT在变换区间内，等间隔采样序列频谱函数的采样间隔。**

类比理解：
- **时域采样**：以时间为基本单位，每隔 $T_s$ 秒采样一个点
- **频域采样**：以旋转因子为基本单位，每隔 $\frac{2\pi}{N}$ 弧度采样一个频谱点

比如 $N=4$ 要在频域里采样4个点，以什么为采样单位来贯穿这4个点呢？

刚好傅里叶变换中有能表达为复指数的形式，而在复平面这个单位圆里就平均分为 $N$ 等分，那一份就是频率采样的单位。

4点DFT中，频率采样的第一个点就是 ![[../kb_material/FPGA_image/dft_w_factor_3.png]] ，第二个点就是 $W_4^2$，以此类推。而**旋转因子**就是表达这个**等间隔采样序列的频谱函数**。

## 欧拉公式联系

复指数与三角函数的连接是**欧拉公式**：

$$e^{-j\theta} = \cos\theta - j\sin\theta$$

因此旋转因子可以展开为：

$$W_N^{nk} = \cos\left(\frac{2\pi nk}{N}\right) - j\sin\left(\frac{2\pi nk}{N}\right)$$

## 常用旋转因子值表

### N=2

| $k$ | $W_2^k$ | 值 |
|:---:|:------:|:--:|
| 0 | $e^0$ | $1$ |
| 1 | $e^{-j\pi}$ | $-1$ |

### N=4

| $k$ | $W_4^k$ | 值 |
|:---:|:------:|:--:|
| 0 | $e^0$ | $1$ |
| 1 | $e^{-j\pi/2}$ | $-j$ |
| 2 | $e^{-j\pi}$ | $-1$ |
| 3 | $e^{-j3\pi/2}$ | $j$ |

### N=8

| $k$ | $W_8^k$ | 值（近似） |
|:---:|:------:|:---------:|
| 0 | $e^0$ | $1$ |
| 1 | $e^{-j\pi/4}$ | $\frac{\sqrt{2}}{2} - j\frac{\sqrt{2}}{2} \approx 0.707 - 0.707j$ |
| 2 | $e^{-j\pi/2}$ | $-j$ |
| 3 | $e^{-j3\pi/4}$ | $-\frac{\sqrt{2}}{2} - j\frac{\sqrt{2}}{2} \approx -0.707 - 0.707j$ |
| 4 | $e^{-j\pi}$ | $-1$ |

## 重要性质

### 1. 周期性

$$W_N^{n+N} = W_N^n$$

$$W_N^{nk+N} = W_N^{nk}$$

旋转因子以 $N$ 为周期循环，这在FPGA实现中意味着只需存储一个周期的查找表。

### 2. 对称性

$$W_N^{n+N/2} = -W_N^n$$

这是**半周期反对称**，FFT蝶形运算的核心优化依据——后半部分的旋转因子只需取负号即可得到。

### 3. 可约性

$$W_{mN}^{mn} = W_N^n$$

## 在DFT公式中的角色

N点DFT定义为：

$$X(k) = \sum_{n=0}^{N-1} x(n) \cdot W_N^{nk}$$

旋转因子 $W_N^{nk}$ 作为加权系数，将时域序列 $x(n)$ 投影到第 $k$ 个频域采样点上。

## FPGA实现要点

### 存储优化：查找表（LUT）

利用旋转因子的**周期性**和**对称性**，只需存储 $N/4$ 个值（第一象限），其余通过符号变换和索引映射获得：

- 第二象限：$W_N^{N/4+k} = -j \cdot W_N^k$（实部虚部交换并变号）
- 第三象限：$W_N^{N/2+k} = -W_N^k$（取负）
- 第四象限：$W_N^{3N/4+k} = j \cdot W_N^k$

### CORDIC算法

对于需要动态计算旋转因子的场景（如变长FFT），可使用**CORDIC算法**直接在硬件中计算 $\sin/\cos$，无需查找表，但速度较慢。

### 量化精度

旋转因子通常用**定点数**表示：
- 16-bit Q15 格式：精度 $\approx 2^{-15} \approx 3 \times 10^{-5}$，适用于大多数应用
- 18-bit/20-bit：高精度需求（如雷达信号处理）
- 浮点（FPGA软核或DSP48）：最高精度但资源消耗大

---

## 相关笔记

- [[信号处理/数字下变频（DDC）]] — 正交混频中同样用到复指数旋转
- [[信号处理/空间谱估计与 MUSIC 算法]] — 阵列信号处理中的DFT/FFT应用
