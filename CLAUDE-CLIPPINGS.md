# CLAUDE-CLIPPINGS.md

> Obsidian Web Clipper 抓取文章的整理操作手册。
> 当用户说"整理抓取的文章"或"整理 Clippings"时，严格按此流程执行。

---

## 〇、意图识别

**核心原则：用户不需要记住任何命令词，用自然语言表达即可，Claude 自动识别意图并执行。**

### 0.1 触发词识别

以下表述都应触发整理流程（不限于这些）：

| 用户可能说的话 | 识别结果 |
|---------------|---------|
| "整理抓取的文章" | 完整整理流程 |
| "整理 Clippings" | 完整整理流程 |
| "帮我把抓的文章归类一下" | 完整整理流程 |
| "Clippings 里有新东西吗" | 先检查，有则整理 |
| "把这篇文章翻一下" | 仅翻译 + 格式化 |
| "这篇帮我分类" | 仅分类 + 移动 |
| "这篇文章帮我整理一下" | 完整整理流程 |
| "Clippings 清理一下" | 完整整理流程（含丢弃判断） |
| "最近抓的文章处理一下" | 完整整理流程 |
| "这篇英文的帮我翻成中文" | 仅翻译 + 格式化 |

### 0.2 部分操作识别

用户可能只想做部分操作，而非完整流程。识别后**只执行用户要求的部分**：

| 用户意图 | 执行动作 |
|---------|---------|
| "翻译" | Step 4（翻译）+ Step 5（格式化） |
| "分类/归类" | Step 6（分类）+ Step 10（移动） |
| "整理格式" | Step 5（标准化输出格式） |
| "加标签" | Step 7（Frontmatter） |
| "关联笔记" | Step 8（Wiki 链接） |
| "建个新分类" | Step 6 中的自动创建新分类 |
| "丢掉/删除" | Step 3 质量评估 + 删除 |

### 0.3 模糊意图处理

当用户表述模糊时（如"看看这个"、"这个怎么办"），**主动询问**：

```
检测到你在看一篇抓取的文章，需要我帮你：
1. 完整整理（翻译+分类+格式化+关联）
2. 仅翻译为中文
3. 仅归类到合适的目录
4. 其他（请说明）
```

### 0.4 多文章处理

当用户说"把这些都整理一下"或"全部处理"：
- 逐一处理 `docs/Clippings/` 中所有文件
- 每篇独立执行完整流程
- 最后汇总输出整理结果

### 0.5 上下文感知

- 如果用户正在看某篇具体文章（通过 editor_selection 或 linked_note），直接对该文章操作
- 如果用户没有指定文章，默认处理 `docs/Clippings/` 中所有未整理的文件
- 如果用户提到某个具体主题（如"关于 FPGA 的那篇"），根据文件名或内容匹配

---

## 一、Clippings 是什么

**来源**：用户通过 **Obsidian Web Clipper**（浏览器扩展）从网页抓取文章，自动保存到 `docs/Clippings/` 目录。

**典型内容**：技术博客、教程、论文摘要、GitHub README、知乎/掘金文章等。

**典型文件名格式**：
```
文章标题 (网站名).md
Example: 理解 Transformer 的注意力机制 (知乎).md
```

**典型内容结构**（Web Clipper 生成）：
```markdown
---
source: https://...
author: ...
created: ...
tags: [...]
---

# 文章标题

正文内容（可能包含图片、代码块、表格等）
```

---

## 二、完整整理流程

### Step 1：发现新文件

```bash
# 列出 Clippings 目录下所有文件
ls -lt docs/Clippings/
```

- 如果目录为空或只有旧文件（已在其他目录），告知用户"Clippings 目录没有新文章"
- 如果有新文件，逐一处理

### Step 2：读取并理解内容

对每篇文章执行：

1. **Read** 完整文件内容
2. 识别文章的**主题领域**（见下方分类规则）
3. 评估**内容质量**（见下方质量标准）
4. 判断是否**值得保留**

### Step 3：质量评估

#### 保留标准（满足任一即可）

- ✅ 有实质性技术内容（原理讲解、代码示例、实践经验）
- ✅ 与现有笔记主题相关，能补充或扩展已有知识
- ✅ 包含独特的见解、总结或对比分析
- ✅ 是权威来源（官方文档、知名博主、经典论文）

#### 丢弃标准（满足任一即丢弃）

- ❌ 纯新闻/资讯（无长期参考价值）
-  广告/推广内容
- ❌ 内容过于浅显（一句话能概括的）
- ❌ 与知识库主题完全无关
- ❌ 内容已被现有笔记完全覆盖且无新增价值
- ❌ 文件为空或仅含 frontmatter

**丢弃操作**：直接 `rm` 删除文件，并在最终汇总中告知用户丢弃了哪些及原因。

### Step 4：内容翻译（英文→中文）

**核心原则：所有内容最终输出为中文。**

#### 4.1 必须翻译的部分

- 文章标题 → 中文
- 正文段落 → 中文（保留专业术语原文，如 Transformer、FPGA、FFT）
- 小标题/章节名 → 中文
- 表格中的文字 → 中文
- 图片说明/alt 文本 → 中文
- 代码注释 → 中文（代码本身不翻译，只翻译注释）

#### 4.2 保留英文不翻译的部分

- **专业术语**：Transformer、CNN、LSTM、FPGA、Verilog、UART、SPI、I2C、FFT、DFT 等
- **工具/框架名**：PyTorch、TensorFlow、Vivado、Quartus、VCS、Design Compiler 等
- **代码内容**：变量名、函数名、关键字（`always`、`module`、`endmodule` 等）
- **数学符号和公式**：保持原样
- **缩写**：RTL、DFT、UPF、DRC、LVS、STA 等（首次出现时在括号中给出中文全称）

#### 4.3 翻译风格

- 用**简洁的中文技术写作风格**，不要机翻腔
- 术语首次出现时标注英文原文，如："寄存器传输级（Register-Transfer Level, RTL）"
- 被动语态转为主动语态
- 长句拆分为短句

---

### Step 5：内容清理与标准化输出格式

通过质量评估并翻译后的文章，按以下**标准输出格式**重构：

#### 5.1 标准笔记结构（必须按此顺序）

```markdown
---
tags: [分类, 标签1, 标签2]
source: "原始URL"
author: "原作者"
created: YYYY-MM-DD
---

# 文章标题（中文）

> 📋 **一句话摘要**：用 1~2 句话概括本文核心内容。

---

## 核心概念

> 列出文章中涉及的 3~5 个核心概念，每个用一句话解释。
> 如果概念较难，紧跟一条大白话解释。

## 正文梳理

### 小标题1
正文内容...

> 💡 **通俗理解**：用生活化的比喻或大白话解释上面的内容。

### 小标题2
正文内容...

### 小标题3
正文内容...

## 关键要点总结

- 要点1
- 要点2
- 要点3

## 延伸阅读

- [[相关笔记1]] — 关联说明
- [[相关笔记2]] — 关联说明
```

#### 5.2 各部分填写规则

| 部分 | 规则 |
|------|------|
| **一句话摘要** | 必须写！放在文首 blockquote 中，让读者 3 秒了解文章价值 |
| **核心概念** | 提取 3~5 个，每个一行，格式：`**术语**：定义`。难懂的概念加 `> 💡 大白话：...` |
| **正文梳理** | 按原文逻辑分段，但**重新组织为清晰的层级**。删除冗余，合并重复。每段后如果概念较难，加通俗解释 |
| **关键要点总结** | 文末必须有一个 bullet list，3~6 条，提炼全文最核心的结论 |
| **延伸阅读** | 用 Wiki 链接关联知识库中相关笔记 |

#### 5.3 通俗解释的写法

当遇到**难懂的技术概念**时，用以下格式添加解释：

```markdown
> 💡 **大白话**：[用生活化的比喻解释]

> 📝 **举个例子**：[用具体例子说明]

> ⚡ **一句话记住**：[最精炼的总结]
```

**通俗解释的原则**：
- 用**生活中的事物**做类比（做菜、开车、盖房子、打电话...）
- 避免嵌套术语（解释中不要用另一个难懂术语）
- 控制长度：大白话 1~2 句，例子 2~3 句
- 放在紧跟着难懂概念的后面，不要集中放在文末

#### 5.4 图片处理（重要）

当文章中包含截图、示意图、架构图等图片时，必须按以下规则处理：

##### 5.4.1 图片存储位置

**所有图片统一存储到 `knowledge_base_material/` 目录下**，按内容主题自动创建子文件夹。

```
knowledge_base_material/
├── 文章标题/                  ← 以文章标题（中文）创建文件夹
│   ├── 架构图.png              ← 根据图片内容命名
│   ├── 时序图.png
│   └── 代码截图.png
├── 另一篇文章标题/
│   └── ...
└── ...
```

##### 5.4.2 文件夹命名规则

- 以**整理后的文章标题**（中文）创建文件夹
- 如果多篇文章属于同一主题，可以合并到一个文件夹中
- 文件夹名用中文，简洁明了

##### 5.4.3 图片文件命名规则

- **根据图片内容命名**，不要用原始文件名（通常是随机字符串）
- 命名格式：`[图片描述].png` 或 `[图片描述].jpg`
- 示例：
  ```
  原始文件名：Pasted image 20260723220455.png
  图片内容：UART 数据帧格式时序图
  重命名为：UART数据帧格式.png
  ```

##### 5.4.4 图片判断标准

**需要保留的图片**：
- ✅ 技术架构图、系统框图
- ✅ 时序图、波形图
- ✅ 流程图、状态机图
- ✅ 代码截图（如果代码本身已保留为文本，截图可删除）
- ✅ 实验结果图、数据图表
- ✅ 对比表格截图

**可以删除的图片**：
- ❌ 作者头像、公众号二维码
- ❌ 网页广告、推广图
-  装饰性插图（与内容无关）
- ❌ 重复的图片（文章中多次出现同一张图）

##### 5.4.5 图片下载与引用

处理步骤：

```bash
# 1. 在 knowledge_base_material/ 下创建文章对应文件夹
mkdir -p "knowledge_base_material/文章标题/"

# 2. 下载图片到该文件夹（如果图片有 URL）
curl -sfo "knowledge_base_material/文章标题/图片描述.png" "图片URL"

# 3. 如果是 Obsidian 粘贴的图片（已在 vault 中），直接移动
mv "docs/Pasted image xxx.png" "knowledge_base_material/文章标题/图片描述.png"
```

在笔记中引用图片：

```markdown
<!-- 正确：使用 wiki 链接引用 kb_material 中的图片 -->
![[knowledge_base_material/文章标题/图片描述.png]]

<!-- 或者用相对路径（如果图片在同级目录） -->
![[图片描述.png]]

<!-- 错误：使用外部 URL -->
![](https://example.com/image.png)
```

##### 5.4.6 图片丢失时的处理

如果图片无法下载或已失效：
- 用文字描述替代：`> 🖼️ 原图：[图片内容描述]`
- 如果是关键示意图，标注"⚠️ 原图已失效，待补充"

##### 5.4.7 图片整理示例

假设整理一篇关于"UART 串口通信"的文章，其中包含 3 张截图：

```
原始文件：
  docs/Clippings/UART串口通信详解 (知乎).md
  docs/Pasted image 20260723120000.png  （UART 数据帧格式图）
  docs/Pasted image 20260723120001.png  （UART 收发时序图）
  docs/Pasted image 20260723120002.png  （作者公众号二维码）

整理后：
  knowledge_base_material/UART串口通信详解/
    ├── UART数据帧格式.png
    └── UART收发时序图.png
  
  docs/FPGA/UART串口通信详解.md
    内容中引用：
    ![[knowledge_base_material/UART串口通信详解/UART数据帧格式.png]]
    ![[knowledge_base_material/UART串口通信详解/UART收发时序图.png]]
  
  （公众号二维码已删除）
```

#### 5.5 删除无用内容

- 删除网页导航栏、侧边栏、页脚等残留文本
- 删除"相关推荐"、"版权声明"、"扫码关注"等模块
- 删除无意义的空行（保留段落间的合理空行）
- 删除 `![](url)` 形式的远程图片链接（图片已按 5.4 规则处理）
- 删除原文中的"目录"、"Table of Contents"等导航

#### 5.6 保留并优化的内容

- 文章标题转为一级标题 `#`（中文）
- 保留正文层级结构，但**重新梳理为逻辑清晰的层次**（`##` `###`）
- 保留所有代码块（标注语言类型，注释翻译为中文）
- 保留核心示意图（按 5.4 规则下载并重命名）
- 保留表格数据（表头翻译为中文）
- 保留公式（确保 LaTeX 格式 `$...$` 或 `$$...$$`）

### Step 6：确定分类与目标路径

#### 分类规则

| 主题关键词 | 目标目录 | 说明 |
|-----------|---------|------|
| 深度学习、神经网络、CNN、RNN、LSTM、Transformer、PyTorch、训练、推理 | `深度学习/` | ML 理论与框架 |
| 机器学习、SVM、决策树、聚类、特征工程、数据预处理 | `ML/` | 传统 ML |
| FPGA、Verilog、VHDL、时序约束、IP 核、UART、SPI、I2C、综合、布局布线 | `FPGA/` | FPGA 开发 |
| 数字IC、芯片设计、验证、UVM、SystemVerilog、综合、后端、流片 | `数字IC/` | 数字 IC 设计 |
| DFT、FFT、滤波器、采样、频谱、MUSIC、DOA、雷达、信号检测 | `信号处理/` | 信号处理理论 |
| 对消、LMS、RLS、自适应、干扰抑制 | `数字对消/` | 对消算法 |
| Python、NumPy、Pandas、数据处理 | `Pytorch入门/` | Python 基础 |
| 软件使用、工具配置、效率技巧 | `软件秘籍/` | 软件技巧 |

#### 自动创建新分类

**如果文章不属于以上任何分类，自动创建新的分类目录。**

创建规则：
1. 根据文章核心主题确定**简洁的中文目录名**（如 `嵌入式/`、`通信协议/`、`数学基础/`、`论文阅读/`）
2. 在 `docs/` 下创建该目录
3. 在目录中创建 `index.md` 索引文件：
   ```markdown
   ---
   tags:
     - 索引
   created: YYYY-MM-DD
   ---

   # 目录名

   简要描述这个分类包含的内容。
   ```
4. 在 `docs/index.md` 中添加新的分类小节
5. 在 `mkdocs.yml` 的 `nav` 中添加新的分类条目
6. 告知用户"已创建新分类：XX/"

**判断逻辑**：
```
文章主题 → 匹配现有分类？
  ├─ 是 → 归入现有目录
  └─ 否 → 该主题是否值得独立分类？
           ├─ 是（≥2篇同主题或主题明确）→ 创建新目录
           ─ 否（仅1篇且主题模糊）→ 归入最接近的现有目录，并标注建议
```

#### 文件命名规范

```
规则：使用简洁的中文名，不加来源后缀
示例：
  "理解 Transformer 的注意力机制 (知乎).md"
  → "Transformer 注意力机制详解.md"

  "Getting Started with Vivado Block Design (Xilinx).md"
  → "Vivado Block Design 入门.md"

  "A Comprehensive Guide to LMS Algorithm.md"
  → "LMS 自适应算法详解.md"
```

**命名原则**：
- 用中文命名，专业术语可保留英文
- 简洁明了，能概括文章核心内容
- 不加来源、不加日期、不加编号
- 如果与现有笔记同名，加副标题区分（如 `LMS 算法（自适应滤波）.md`）

### Step 7：创建标准 Frontmatter

每篇整理后的笔记必须包含：

```yaml
---
tags:
  - 一级分类（如：深度学习、FPGA、信号处理）
  - 二级标签（如：Transformer、注意力机制）
source: "原始 URL"
author: "原作者名（如有）"
created: YYYY-MM-DD
---
```

**标签规则**：
- 第一个 tag 必须是所属目录的一级分类（用于索引）
- 后续 tags 是文章的具体主题（用于搜索）
- tag 数量控制在 2~5 个

### Step 8：关联现有笔记

整理时主动检查：

```bash
# 搜索知识库中是否有相关笔记
grep -rl "关键词" docs/ --include="*.md" | grep -v Clippings
```

- 如果找到相关笔记，在整理后的文章中用 `[[目录/笔记名]]` 添加引用
- 同时在**已有笔记**中也反向添加对新笔记的引用（如果合适）

### Step 9：更新索引与导航

#### 9.1 更新 `docs/index.md`

在对应分类下添加一行：
```markdown
- [文章标题](目录/文章标题.md) — 一句话描述文章核心内容
```

#### 9.2 更新 `mkdocs.yml`

在对应分类的 `nav` 下添加：
```yaml
  - 分类名:
      - 文章标题: 目录/文章标题.md
```

#### 9.3 更新 `FPGA/index.md`（如果归入 FPGA）

在对应小节下添加 Wiki 链接。

### Step 10：移动文件

```bash
mv "docs/Clippings/原标题 (来源).md" "docs/目标目录/新标题.md"
```

### Step 11：构建验证

```bash
cd D:\Obsidian\my-knowledge-base
mkdocs build
```

确保无报错。如有 broken link 或 missing file 警告，立即修复。

---

## 三、特殊内容类型处理

### 3.1 代码密集型文章

- 保留所有代码块，确保标注语言（` ```python `、` ```verilog `）
- 如果代码有注释，保留注释
- 如果代码过长（>100行），考虑只保留核心片段，标注"完整代码见原文"
- 添加代码的解释说明（如果原文没有）

### 3.2 论文/学术文章

- 提取核心贡献和创新点
- 保留关键公式（转为 LaTeX 格式）
- 保留实验结果表格
- 添加"与现有方法对比"的总结
- 标记引用来源

### 3.3 图文混合文章

**严格按 Step 5.4（图片处理）规则执行**：
- 根据图片内容自动创建 `knowledge_base_material/文章标题/` 文件夹
- 下载图片并按内容重命名（不用原始随机文件名）
- 在笔记中用 `![[knowledge_base_material/文章标题/xxx.png]]` 引用
- 删除无关图片（头像、广告、二维码）
- 无法下载的图片用文字描述替代：`>  原图：XXX的示意图`

### 3.4 教程/步骤类文章

- 保留完整的步骤编号
- 每个步骤添加简短说明
- 如果有截图，保留截图或文字替代描述
- 添加"前置知识"和"后续延伸"链接

### 3.5 对比/总结类文章

- 保留对比表格
- 提取核心结论
- 如果与其他笔记主题重叠，在相关笔记中添加引用

---

## 四、批量整理时的输出格式

整理完成后，向用户汇报：

```
## 整理结果

### ✅ 已归类（N 篇）

| 原标题 | 新标题 | 目标目录 | 核心主题 | 翻译 |
|--------|--------|---------|---------|------|
| 原标题1 | 新标题1 | 深度学习/ | Transformer 注意力机制 | 英→中 |
| 原标题2 | 新标题2 | FPGA/ | UART 波特率计算 | 英→中 |

### 📁 新建分类（M 个）

| 新目录 | 包含文章 | 创建原因 |
|--------|---------|---------|
| 嵌入式/ | 文章A、文章B | 2篇嵌入式主题，无匹配现有分类 |

### ❌ 已丢弃（K 篇）

| 原标题 | 丢弃原因 |
|--------|---------|
| 标题3 | 纯新闻，无长期参考价值 |
| 标题4 | 内容与 [[已有笔记]] 完全重复 |

### 📝 已关联

- [[新笔记1]] ← 关联到 [[已有笔记A]]、[[已有笔记B]]
- [[新笔记2]] ← 关联到 [[已有笔记C]]

### 💡 通俗解释已添加

- [[新笔记1]]：3 个大白话解释（注意力机制、多头、位置编码）
- [[新笔记2]]：2 个大白话解释（波特率、采样位置）

### ⚠️ 需要手动处理

- "标题5" — 无法确定分类，建议归入 XX 目录
```

---

## 五、注意事项

1. **全部输出中文** — 正文、标题、注释、表格全部翻译为中文，仅保留专业术语英文
2. **必须写一句话摘要** — 放在文首 `> 📋` 中，让读者 3 秒了解文章价值
3. **必须有要点总结** — 文末必须有 bullet list 提炼 3~6 条核心结论
4. **难懂概念加通俗解释** — 用 `> 💡 大白话：` 或 `> 📝 举个例子：` 格式，紧跟在难懂概念后面
5. **不要修改原文的核心观点** — 只做格式清理、翻译和结构优化，不改变作者本意
6. **标注补充内容** — 如果添加了原文没有的解释，用 `> 💡` 或 `> 📝 补充：` 标注
7. **保持简洁** — 整理后的笔记应该比原文更精炼，去掉冗余，保留精华
8. **一次处理一篇** — 不要同时打开太多文件，避免混淆
9. **先预览再移动** — 移动文件前先确认目标路径正确
10. **索引必须同步** — index.md 和 mkdocs.yml 必须同时更新，缺一不可
11. **构建必须通过** — 每次整理后运行 `mkdocs build` 验证
12. **新分类自动创建** — 如果文章不属于任何现有分类，自动创建新目录并配置索引
13. **图片必须归档** — 所有图片统一存到 `knowledge_base_material/文章标题/` 下，按内容命名，笔记中用 `![[...]]` 引用

---

## 六、整理笔记的标准模板

以下是一篇整理后的笔记应该呈现的**完整结构示例**：

````markdown
---
tags:
  - FPGA
  - UART
  - 串行通信
source: "https://example.com/uart-guide"
author: "原作者名"
created: 2026-07-23
---

# UART 串口通信详解

> 📋 **一句话摘要**：UART 是一种异步串行通信协议，只需两根线（TX/RX）即可实现设备间的数据传输，是嵌入式系统中最常用的调试接口。

---

## 核心概念

- **异步通信**：发送方和接收方没有共享时钟线，靠约定好的"波特率"（传输速度）来同步
- **波特率（Baud Rate）**：每秒传输的比特数，如 9600、115200
- **起始位/停止位**：每帧数据的"包头"和"包尾"，告诉接收方一帧何时开始、何时结束
- **全双工**：收发可以同时进行（两根线各管一个方向）

> 💡 **大白话**：UART 就像两个人打电话——不需要对表（没有时钟线），只要提前说好语速（波特率），各说各听就行。一根线说、一根线听。

---

## 正文梳理

### 数据帧格式

每一帧数据由以下部分组成：

| 部分 | 电平 | 说明 |
|------|------|------|
| 空闲 | 高 (1) | 没数据传输时保持高电平 |
| 起始位 | 低 (0) | 告诉接收方"数据来了" |
| 数据位 | 0 或 1 | 实际数据，通常 8 位，低位先发 |
| 校验位 | 0 或 1 | 可选，用于简单检错 |
| 停止位 | 高 (1) | 标记一帧结束 |

> 💡 **大白话**：想象你在寄快递——起始位是"我要发货了"的通知，数据位是包裹里的东西，停止位是"发完了"的确认。校验位就像快递单号，用来确认包裹没丢。

### 波特率与采样

波特率决定了传输速度。接收方在每个 bit 的**中间位置**采样，因为那里信号最稳定。

> 📝 **举个例子**：如果波特率是 115200，每个 bit 持续约 8.68 微秒。接收方在 4.34 微秒处采样——离两边跳变都最远，最不容易采错。

### Verilog 实现要点

```verilog
// 波特率计数器：每个 bit 需要计数的时钟周期
// 50MHz / 115200 ≈ 434 个时钟周期
localparam BIT_PERIOD = CLK_FREQ / BAUD_RATE;

// 发送：按波特率节奏逐位输出
// 接收：检测下降沿（起始位），然后每隔 BIT_PERIOD 采样一次
```

---

## 关键要点总结

- UART 只需 2 根线（TX/RX），是最简单的串行通信方式
- 收发双方波特率必须一致（误差 < 3%）
- 数据帧 = 起始位 + 数据位 + (校验位) + 停止位
- 接收方在 bit 中间采样，最远离跳变沿
- 异步 = 无时钟线，靠起始位同步
- 全双工 = 收发同时进行

## 延伸阅读

- [[FPGA/SPI串行通信协议]] — 同步高速四线协议，比 UART 快但线更多
- [[FPGA/I2C串行通信协议]] — 两线半双工，支持多设备地址寻址
- [[FPGA/数字芯片设计与验证全流程]] — UART 作为 IP 核在芯片设计流程中的位置
````

---

## 七、快速参考：目录分类速查

```
docs/
├── 深度学习/      ← 神经网络、CNN、RNN、LSTM、Transformer、PyTorch
├── ML/           ← 传统机器学习、SVM、决策树、聚类
├── 信号处理/      ← FFT、滤波器、雷达、DOA、MUSIC、采样
├── FPGA/         ← Verilog、时序、IP核、接口协议、芯片设计流程
├── 数字IC/       ← ASIC设计、验证、UVM、综合、后端、流片
── 数字对消/      ← LMS、RLS、自适应滤波、干扰对消
├── Pytorch入门/   ← Python基础、PyTorch教程
├── 软件秘籍/      ← 工具配置、效率技巧
├── Clippings/    ← 抓取文章暂存（整理后清空）
└── （新分类自动创建）← 不属于以上分类时，按主题自动创建

knowledge_base_material/    ← 图片归档目录（重要！）
├── 文章标题1/
│   ├── 架构图.png
│   └── 时序图.png
├── 文章标题2/
│   ── 代码截图.png
└── ...
```
